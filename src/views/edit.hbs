<form action="/edit/{{task._id}}" method="post">
    <div>
        <label for="title">Title</label>
        <input type="text" name="title" placeholder="Title" id="title" value="{{task.title}}">
    </div>
    <div>
        <label for="description">Description</label>
        <textarea name="description" placeholder="Description" cols="30" rows="10">{{task.description}}</textarea>
    </div>
    <button type="submit" name="subimit">Enviar</button>
</form>